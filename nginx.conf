events { worker_connections 1024; }
http {
    upstream backend_servers {
        server api1:3000;
        server api2:3000;
        server api3:3000;
    }
    server {
        listen 80;
        location / {
            proxy_pass http://backend_servers;
        }
    }
}